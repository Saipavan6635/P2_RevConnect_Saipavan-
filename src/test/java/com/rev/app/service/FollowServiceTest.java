package com.rev.app.service;

import org.junit.Test;

import static org.junit.Assert.assertNotNull;

public class FollowServiceTest {

    @Test
    public void classShouldBeLoadable() {
        assertNotNull(FollowService.class);
    }
}